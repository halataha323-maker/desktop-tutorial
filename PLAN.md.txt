# Project Plan - Assignment 03

* **שלב 1:** אפיון המערכת והקמת תשתיות התיעוד וה-Git (השלב הנוכחי).
* **שלב 2:** הגדרת פרויקט ב-Google Cloud Console, הפעלת ה-APIs (Gmail & Calendar) והורדת `credentials.json`.
* **שלב 3:** פיתוח הקוד בפייתון (חיבור ל-APIs, כתיבת פרומפט ל-LLM, ולוגיקת ה-Free/Busy).
* **שלב 4:** בדיקות מערכת מול מקרי קצה (מיילים תפוסים/ריקים).
* **שלב 5:** הגשת קובץ ה-PDF הייעודי במודל והעלאת הקוד ל-GitHub.